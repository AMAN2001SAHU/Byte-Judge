version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: bytejudge-postgres
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: bytejudge
    ports:
      - "5432:5432"